[project]
name = "remote-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "deepanshu-ctrl", email = "deepanshubirkhani99@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=4.0.3",
]

[project.scripts]
remote-mcp = "remote_mcp:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
